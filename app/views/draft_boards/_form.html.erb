<%= form_with(model: draft_board, local: true) do |form| %>
  <% if draft_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(draft_board.errors.count, "error") %> prohibited this draft_board from being saved:</h2>

      <ul>
      <% draft_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
